<%= form_with(model: employee) do |form| %>
  <% if employee.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(employee.errors.count, "error") %> prohibited this employee from being saved:</h2>

      <ul>
        <% employee.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label t('ُِEmployee.index.name'), style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label t('ُِEmployee.index.email'), style: "display: block" %>
    <%= form.text_field :email %>
  </div>

  <div>
    <%= form.label t('ُِEmployee.index.job'), style: "display: block" %>
    <%= form.text_field :job %>
  </div>

  <div>
    <%= form.label t('ُِEmployee.index.salary'), style: "display: block" %>
    <%= form.text_field :salary %>
  </div>

  <div>
    <%= form.label t('ُِEmployee.index.employment'), style: "display: block" %>
    <%= form.text_field :employment %>
  </div>

  <div>
    <%= form.label t('ُِEmployee.index.team id'), style: "display: block" %>
    <%= form.select :team_id, options_for_select(@teams.map{|u| [u.name, u.id]}, @employee.team_id), {:prompt => "Select Team"}%> <!--drop down menu to select team_id -->
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

